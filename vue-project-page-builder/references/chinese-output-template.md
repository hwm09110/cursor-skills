# 中文输出模板

在进行较大规模 Vue 页面开发前，先使用这份模板。

```md
## 开发前检查

### 1. 已识别的项目规范
- 技术栈：
- 页面组织方式：
- 状态管理方式：
- 样式方案：
- 接口调用方式：
- 组件书写习惯：

### 2. 可复用组件与代码
- 复用组件/模块 1：`路径`
  复用原因：
- 复用组件/模块 2：`路径`
  复用原因：

### 3. 本次页面实现方案
- 页面拆分思路：
- 准备直接复用的部分：
- 需要新增的部分：

### 4. 是否需要封装新组件
- 结论：需要 / 不需要
- 判断依据 1：
- 判断依据 2：
- 如果封装：组件名称、放置目录、对外 props/emits
- 如果不封装：为什么保留在页面内更合适
```

实现完成后，再使用这份模板输出总结。

```md
## 实现结果

- 已复用组件：
- 新增组件：
- 未封装组件的部分：
- 主要遵循的项目规范：
- 如有取舍，说明原因：
```

如果任务属于实质性的页面开发，再在通用检查单后追加 `page-analysis-templates.md` 中对应的页面类型专项模板。
